---

vault_version: "1.12.0"
# vault_server: false

vault_volume: true
vault_volume_size: 35%VG
vault_volume_name: vault
vault_volume_fs_type: "xfs"
vault_volume_mount: /opt/vault/data

vault_role_debug: false
